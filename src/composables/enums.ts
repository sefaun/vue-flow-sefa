export const planes = {
  vertical: 'vertical',
  horizontal: 'horizontal',
} as const

export const edgePointType = {
  input: 'input',
  output: 'output',
} as const

export const mouseButtons = {
  leftButton: 0,
  wheelButton: 1,
  rightButton: 2,
} as const
